services:
  - type: web
    name: dark-store
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: DB_NAME
        value: darkstore
